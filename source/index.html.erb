---
title: Top Open-Source E-Commerce Platforms
---
<%= partial "includes/navbar", :locals => {:controls => true} %>
<div class="main landing">
  <ul class="projects">
    <% projects.each_with_index do |project, index| %>
      <li class="project lang-<%= project.language.downcase.gsub(/\s/, '-').gsub('.', '') %>"
          data-stars="<%= project.stars %>"
          data-forks="<%= project.forks %>"
          data-issues="<%= project.issues %>"
          data-title="<%= project.title %>"
          data-language="<%= project.language.downcase.gsub(/\s/, '-') %>">
        <div class="card">
          <h4 class="title"><a href="<%= project.url %>"><%= project.title %></a></h4>
          <h6 class="url">
            <% if project.homepage != "N/A" %>
              <a href="<%= project.homepage %>" target="_blank"><i class="fa fa-home"></i> Homepage </a>
            <% end %>
            <% if project.demo != "N/A" %>
              <a href="<%= project.demo %>" target="_blank"><i class="fa fa-home"></i> Demo </a>
            <% end %>
            <a href="https://github.com/<%= project.repo %>" target="_blank"><i class="fa fa-github"></i> GitHub</a>
          </h6>
          <ul class="stats">
            <li title="Stars">
              <i class="fa fa-star"></i>
              <span class="stat"><%= project.stars %></span>
              <span class="trend <%= project.stars_trend %>" title="Difference since last week">
                <%= project.stars_diff %>
                <%= caret_icon(project.stars_diff) %>
              </span>
            </li>
            <li title="Forks">
              <i class="fa fa-code-fork"></i>
              <span class="stat"><%= project.forks %></span>
              <span class="trend <%= project.forks_trend %>" title="Difference since last week">
                <%= project.forks_diff %>
                <%= caret_icon(project.forks_diff) %>
              </span>
            </li>
            <li title="Open Issues">
              <i class="fa fa-bug"></i>
              <span class="stat"><%= project.issues %></span>
              <span class="trend" title="Difference since last week">
                <%= project.issues_diff %>
                <%= caret_icon(project.issues_diff) %>
              </span>
            </li>
          </ul>
          <dl>
            <dt style="width: 46%;">First Release:</dt>
            <dd style="width: 54%;" class="datetime-stat"><time class="timeago" datetime="<%= project.created_at %>"><%= project.created_at.to_datetime.strftime("%A, %B %d, %Y %T") %></time></dd>

            <dt style="width: 46%;">Last activity:</dt>
            <dd style="width: 54%;" class="datetime-stat"><time class="timeago" datetime="<%= project.updated_at %>"><%= project.updated_at.to_datetime.strftime("%A, %B %d, %Y %T") %></time></dd>

            <dt style="width: 46%;">Last commit:</dt>
            <dd style="width: 54%;" class="datetime-stat"><time class="timeago" datetime="<%= project.pushed_at %>"><%= project.pushed_at.to_datetime.strftime("%A, %B %d, %Y %T") %></time></dd>
          </dl>
          <div style="margin: 16px -18px; border-bottom: 1px solid #eee;"></div>
          <div class="description"><%= project.description %></div>
          <div style="margin: 16px -18px; border-bottom: 1px solid #eee;"></div>
          <dl>
            <dt>Language:</dt>
            <dd><%= project.language %></dd>

            <dt>Framework:</dt>
            <dd><%= project.framework %></dd>

            <dt>License:</dt>
            <dd><%= project.license %></dd>
          </dl>
        </div>
      </li>
    <% end %>
  </ul>
</div>
